name: Compile ESP32 Firmware

on:
  push:
    branches:
      - main  # Triggers the compilation every time you push to the main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Arduino CLI
      uses: arduino/setup-arduino-cli@v2

    - name: Install ESP32 Platform Core
      run: |
        arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
        arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

    - name: Install Libraries
      run: |
        arduino-cli lib install "Adafruit GFX Library"
        arduino-cli lib install "Adafruit SH110X"

    - name: Compile Sketch
      run: |
        # Replace 'esp.txt' with the actual name of your .ino file if you rename it
        # Compiles specifically for the ESP32 Dev Module
        arduino-cli compile --fqbn esp32:esp32:esp32 esp.txt --output-dir ./build

    - name: Commit and Push Compiled Binary
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        mkdir -p main
        cp build/esp.txt.bin firmware.bin
        git add firmware.bin
        git commit -m "Automated firmware compilation" || exit 0
        git push

